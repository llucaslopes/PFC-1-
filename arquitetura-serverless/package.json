{
  "name": "tcc-arquitetura-serverless",
  "version": "0.1.0",
  "private": true,
  "description": "A3 do TCC: arquitetura serverless (Vercel Functions + Vercel KV) recebendo amostras do ESP32 via Wi-Fi.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev --listen 3001",
    "deploy:preview": "vercel",
    "deploy:prod": "vercel --prod",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "@vercel/node": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.2",
    "vercel": "^36.0.0"
  }
}
